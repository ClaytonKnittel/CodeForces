
#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>


int main(int argc, char * argv[]) {

    int t, n;

    fscanf(stdin, "%d", &t);
    
    while (t--) {
        fscanf(stdin, "%d", &n);

        int prev = 0;

        for (int i = 0; i < n; i++) {
            int num;
            fscanf(stdin, "%d", &num);

        }

    }

    return 0;
}


